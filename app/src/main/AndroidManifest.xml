<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.emergency_android_app">

    <!-- Permissions for storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permissions for SMS, Location, and Phone Calls -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Optional: For accessing contacts (if required by your app) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Optional: For accessing the internet (if required by your app) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Feature requirements -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- Query for Google Maps (optional, if needed) -->
    <queries>
        <!-- Explicitly declare that the app can query for Google Maps -->
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmergencyAndroidApp"
        tools:targetApi="31">

        <!-- Main Activity (entry point of the app) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Activity (new activity to add) -->
        <activity android:name=".ProfileActivity"
            android:exported="true">
            <!-- Add intent filters here if needed -->
        </activity>

        <!-- FileProvider for handling images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.emergency_android_app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
